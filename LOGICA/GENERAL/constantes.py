import yaml
from os import path

def leer_config():
    fn_config = 'config.yml'
    f_config = open(fn_config,'r+')
    config = yaml.load(f_config, Loader = yaml.SafeLoader)

    return config

v_config = leer_config()


################################################################################################################
# RUTAS
################################################################################################################
P_ACCESOS = path.join(v_config['base'],v_config['files']['credentials'])

################################################################################################################
# CONEXION SQL
################################################################################################################
N_SERVIDOR = v_config['sql_server']
N_BBDD = v_config['sql_bbdd']

################################################################################################################
# ARCHIVOS
################################################################################################################
FN_CREDENTIALS = 'credentials.json'
FN_AUTH_USER = 'authorized_user.json'

################################################################################################################
# QUERIES
################################################################################################################
Q_ALUMNOS = '''
    SELECT 
        * 
    FROM BD_PROYECTOS.dbo.ALUMNOS;
'''